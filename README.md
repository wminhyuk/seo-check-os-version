'''bash
# install PDM
# git clone ...
$ source .venv/bin/activate
$ pdm install
# $ vi ...
# TEST
$ pdm install
$ pdm test
$ pip install .

$ git add <FILE_NAME>
$ git commit -a
$ git push
$ git publish

#USE

pdm install
python
from seo_check_os_version.hi import hi
hi()


# seo-check-os-version
